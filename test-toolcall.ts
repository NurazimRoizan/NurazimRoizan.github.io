import { generateText, tool } from 'ai'; import { z } from 'zod'; import { google } from '@ai-sdk/google'; async function main() { const result = await generateText({ model: google('gemini-3.1-flash-lite'), prompt: 'email a@a.com with message hi', tools: { sendEmailToNurazim: tool({ description: 'send email', parameters: z.object({ email: z.string(), content: z.string() }) }) } }); console.log(result.toolCalls); } main();
